import Image from 'next/image'
import React from 'react'
// import person from '@/public/person.web'

interface Props {
    
}

const Buddy = (props: Props) => {
    return (
        <div>
            <Image src={''} alt={''}/>
        </div>
    )
}

export default Buddy
